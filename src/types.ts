export type ResultsType = {
  id: string;
  title: string;
  thumbnail: string;
  price: number;
};

export type ProductDetails = {
  id: string;
  title: string;
  thumbnail: string;
  price: number;
  available_quantity: number;
};
